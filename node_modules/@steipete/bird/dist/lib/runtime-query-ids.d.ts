export type RuntimeQueryIdSnapshot = {
    fetchedAt: string;
    ttlMs: number;
    ids: Record<string, string>;
    discovery: {
        pages: string[];
        bundles: string[];
    };
};
export type RuntimeQueryIdSnapshotInfo = {
    snapshot: RuntimeQueryIdSnapshot;
    cachePath: string;
    ageMs: number;
    isFresh: boolean;
};
export type RuntimeQueryIdsOptions = {
    cachePath?: string;
    ttlMs?: number;
    fetchImpl?: typeof fetch;
};
export declare function createRuntimeQueryIdStore(options?: RuntimeQueryIdsOptions): {
    cachePath: string;
    ttlMs: number;
    getSnapshotInfo: () => Promise<RuntimeQueryIdSnapshotInfo | null>;
    getQueryId: (operationName: string) => Promise<string | null>;
    refresh: (operationNames: string[], opts?: {
        force?: boolean;
    }) => Promise<RuntimeQueryIdSnapshotInfo | null>;
    clearMemory(): void;
};
export declare const runtimeQueryIds: {
    cachePath: string;
    ttlMs: number;
    getSnapshotInfo: () => Promise<RuntimeQueryIdSnapshotInfo | null>;
    getQueryId: (operationName: string) => Promise<string | null>;
    refresh: (operationNames: string[], opts?: {
        force?: boolean;
    }) => Promise<RuntimeQueryIdSnapshotInfo | null>;
    clearMemory(): void;
};
//# sourceMappingURL=runtime-query-ids.d.ts.map