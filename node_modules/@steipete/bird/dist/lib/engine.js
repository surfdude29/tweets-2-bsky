export function resolveEngineMode(value) {
    const normalized = (value || 'auto').toLowerCase();
    if (normalized === 'graphql' || normalized === 'sweetistics' || normalized === 'auto') {
        return normalized;
    }
    return 'auto';
}
export function shouldUseSweetistics(engine, _hasApiKey) {
    if (engine === 'sweetistics')
        return true;
    if (engine === 'graphql')
        return false;
    return false; // auto: GraphQL primary, Sweetistics only as fallback
}
export function shouldAllowSweetisticsFallback(engine, hasApiKey) {
    return engine === 'auto' && hasApiKey;
}
//# sourceMappingURL=engine.js.map